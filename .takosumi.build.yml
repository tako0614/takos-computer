apiVersion: v1

metadata:
  id: jp.takos.computer.build
  name: Takos Computer build

nodes:
  web:
    kind: linux-container
    spec:
      image: oven/bun:1.3.14-alpine
      command: bun install --frozen-lockfile && bun run build:all
    dependsOn: []
