version: "0"
jobs:
  - name: build
    steps:
      - name: build-sandbox-host
        run: |
          deno task build:all
          echo "TAKOSUMI_ARTIFACT=sha256:$(sha256sum dist/sandbox-host.js | cut -d ' ' -f 1)"
    artifact:
      name: sandbox-host
