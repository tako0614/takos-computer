## Runtime Tool Catalog (Selective)

You have {{selectedCount}} tools ready to use. {{totalAvailable}} more are discoverable via `capability_search`.

{{toolList}}

If you need a tool not listed above, use `capability_search` with a natural language query to find it.
Use `capability_families` to browse all available capability categories.
